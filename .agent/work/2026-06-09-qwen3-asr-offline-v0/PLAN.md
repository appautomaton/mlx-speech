# PLAN: Qwen3-ASR Offline ASR v0

Generated by auto-plan on 2026-06-09
Active change: 2026-06-09-qwen3-asr-offline-v0
Status: DRAFT

## Goal
Implement [SPEC.md](SPEC.md): Qwen3-ASR offline single-pass ASR v0 for English, Chinese, and mixed Chinese/English speech through the existing `mlx-speech` ASR surface.

## Architecture Approach
Use [DESIGN.md](DESIGN.md) as the architecture contract. Add an isolated `qwen3_asr` model package, a Qwen ASR adapter, and BF16 checkpoint packaging. Keep upstream Qwen source read-only and keep Cohere/Granite behavior stable.

## Execution Routing and Topology
Default: direct, serial, continuation after each slice verification passes.

Overrides:
- Slice 5: subagent recommended because it implements Qwen3 decoder internals across attention, RoPE, QK norm, GQA, and cache behavior.
- Slice 6: subagent recommended because it joins audio encoder, text decoder, placeholder masking, preflight, and greedy generation.

Parallel-safe groups: none.

Checkpoints: none.

## Requirement Traceability
| Requirement | Slices |
| --- | --- |
| R1 family integration | 8, 9 |
| R2 local BF16 checkpoint boundary | 7 |
| R3 audio frontend | 2 |
| R4 prompt and language semantics | 3, 8 |
| R5 MLX runtime model | 4, 5, 6 |
| R6 verification coverage | all slices |
| R7 documentation | 10 |
| AC1 | 8 |
| AC2 | 1 |
| AC3 | 2 |
| AC4 | 3 |
| AC5 | 3 |
| AC6, AC6a | 7 |
| AC7 | 6 |
| AC8, AC9 | 11 |
| AC10 | 10 |
| AC11 | 8 |
| AC12 | all slices |

## Ordered Slice Sequence

### Slice 1: Config And Tokenizer Foundations

**Objective:** Add Qwen3-ASR config parsing and tokenizer asset loading without model inference.

**Acceptance criteria:**
- `Qwen3ASRConfig.from_dir()` reads `models/qwen3_asr_1_7b/original/config.json` and exposes `model_type`, audio token IDs, audio config, text config, and supported languages.
- Unit tests cover the real 1.7B dimensions: audio `num_mel_bins=128`, audio `encoder_layers=24`, text `hidden_size=2048`, text `num_hidden_layers=28`, `num_key_value_heads=8`.
- Tokenizer tests resolve configured audio token IDs and can encode/decode simple English and Chinese text from local tokenizer assets.

**Touches:** `src/mlx_speech/models/qwen3_asr/config.py`, `src/mlx_speech/models/qwen3_asr/tokenizer.py`, `src/mlx_speech/models/qwen3_asr/__init__.py`, `tests/unit/test_qwen3_asr_config.py`, `tests/unit/test_qwen3_asr_tokenizer.py`.

**Produces:** Qwen config/tokenizer primitives that do not import torch, transformers, or qwen-asr.

**Verification:** `.venv/bin/python -m pytest tests/unit/test_qwen3_asr_config.py tests/unit/test_qwen3_asr_tokenizer.py tests/test_runtime_purity.py`

**Status:** complete
**Evidence:** added Qwen3-ASR config/tokenizer primitives and unit tests; `.venv/bin/python -m pytest tests/unit/test_qwen3_asr_config.py tests/unit/test_qwen3_asr_tokenizer.py tests/test_runtime_purity.py` passed with 7 tests; `.venv/bin/python -m pytest tests/unit/` passed with 381 tests.
**Risks / next:** none.

### Slice 2: WhisperFeatureExtractor-Parity Audio Frontend

**Objective:** Implement Qwen3-ASR audio normalization and 128-bin WhisperFeatureExtractor-parity log-mel features.

**Acceptance criteria:**
- Feature extractor handles path-loaded 16 kHz mono audio and NumPy/MLX arrays.
- Unit tests verify `n_fft=400`, `hop_length=160`, 128 mel bins, dynamic/longest padding, attention masks, and `_get_feat_extract_output_lengths` parity.
- Unit tests compare at least one deterministic waveform against an offline-captured upstream WhisperFeatureExtractor reference vector with numeric tolerance, so mel scale and normalization errors cannot pass on shape alone.
- Empty or zero-length waveform input fails with a clear error before feature extraction.
- Non-16 kHz array input fails with a clear resample-before-call error; path input may use existing audio loading.

**Touches:** `src/mlx_speech/models/qwen3_asr/feature_extraction.py`, `tests/unit/test_qwen3_asr_feature_extraction.py`.

**Produces:** Pure NumPy/MLX audio frontend and feature length math.

**Verification:** `.venv/bin/python -m pytest tests/unit/test_qwen3_asr_feature_extraction.py tests/test_runtime_purity.py`

### Slice 3: Processor, Prompt, Placeholder, And Parser

**Objective:** Add Qwen3-ASR processor behavior for explicit token-id prompt construction, audio placeholder expansion, and output parsing.

**Acceptance criteria:**
- `None`, empty string, and `"auto"` build an auto prompt with no forced-language suffix.
- Explicit supported languages append the forced-language text-only suffix.
- Placeholder expansion count exactly matches computed audio feature length for one or more audio inputs.
- Parser tests cover detected language output, forced-language output, empty language output, no-language-tag output, and English/Chinese mixed text output.

**Touches:** `src/mlx_speech/models/qwen3_asr/processor.py`, `tests/unit/test_qwen3_asr_processor.py`.

**Produces:** Reference-equivalent processor behavior without `transformers.apply_chat_template`.

**Verification:** `.venv/bin/python -m pytest tests/unit/test_qwen3_asr_processor.py tests/test_runtime_purity.py`

### Slice 4: Audio Encoder Structure

**Objective:** Implement the MLX Qwen3-ASR audio encoder structure and shape-only forward path.

**Acceptance criteria:**
- Audio encoder includes 3 stride-2 Conv2D stages, `conv_out`, sinusoidal positions, 24 encoder layers from config, `ln_post`, `proj1`, activation, and `proj2`.
- Shape tests verify feature length reduction and output hidden width `2048` for small synthetic inputs.
- Encoder handles single-audio inference; batching beyond the reference-supported path is not introduced.

**Touches:** `src/mlx_speech/models/qwen3_asr/audio_encoder.py`, `tests/unit/test_qwen3_asr_audio_encoder.py`.

**Produces:** Audio encoder module with synthetic forward tests.

**Verification:** `.venv/bin/python -m pytest tests/unit/test_qwen3_asr_audio_encoder.py tests/test_runtime_purity.py`

### Slice 5: Qwen3 Text Decoder

**Objective:** Implement the Qwen3 decoder components needed for ASR greedy generation.

**Acceptance criteria:**
- Decoder implements QK RMSNorm, grouped-query attention, RoPE using config `rope_theta`, MLP, RMSNorm, tied/untied embeddings as required by the checkpoint, and KV cache interfaces.
- Unit tests cover attention shape, KV append, RoPE position behavior, and logits shape for a tiny synthetic config.
- Existing VibeVoice Qwen2 code may guide structure but is not treated as drop-in.

**Execution:** subagent recommended

**Touches:** `src/mlx_speech/models/qwen3_asr/text_decoder.py`, `tests/unit/test_qwen3_asr_text_decoder.py`.

**Produces:** Qwen3 text backbone suitable for multimodal fusion.

**Verification:** `.venv/bin/python -m pytest tests/unit/test_qwen3_asr_text_decoder.py tests/test_runtime_purity.py`

### Slice 6: Multimodal Runtime And Greedy Decode

**Objective:** Join audio frontend, audio encoder, Qwen3 decoder, placeholder masking, context preflight, and greedy generation into an offline single-pass runtime.

**Acceptance criteria:**
- Runtime validates prompt length plus `max_new_tokens` before generation.
- Placeholder count mismatch raises before model forward.
- Greedy decode returns generated token IDs and parsed text/language.
- Synthetic tests exercise embedding replacement and one-step greedy decode without real checkpoint weights.

**Execution:** subagent recommended

**Depends on:** Slices 1-5.

**Touches:** `src/mlx_speech/models/qwen3_asr/model.py`, `src/mlx_speech/generation/qwen3_asr.py`, `tests/unit/test_qwen3_asr_model.py`, `tests/unit/test_qwen3_asr_generation.py`.

**Produces:** Local single-pass Qwen3-ASR runtime skeleton with unit-tested generation mechanics.

**Verification:** `.venv/bin/python -m pytest tests/unit/test_qwen3_asr_model.py tests/unit/test_qwen3_asr_generation.py tests/test_runtime_purity.py`

### Slice 7: BF16 Checkpoint Mapping And Packaging

**Objective:** Add checkpoint loading, alignment reporting, and BF16 packaging from `original/` to `mlx-bf16/`.

**Acceptance criteria:**
- Header/index tests verify Qwen namespaces and both downloaded shards without loading full tensor payloads.
- Sanitizer tests cover `thinker.audio_tower.*`, `thinker.model.*`, `thinker.lm_head.*`, Conv2D layout handling, skipped keys, and duplicate-key detection.
- Conversion script writes `models/qwen3_asr_1_7b/mlx-bf16/` with config/tokenizer files and BF16 safetensors, preserving dtype unless layout transposition is required.
- Checkpoint tests skip cleanly if local model files are absent.

**Depends on:** Slices 1, 4, 5.

**Touches:** `src/mlx_speech/models/qwen3_asr/checkpoint.py`, `scripts/convert/qwen3_asr.py`, `tests/unit/test_qwen3_asr_checkpoint_unit.py`, `tests/checkpoint/test_qwen3_asr_checkpoint.py`.

**Produces:** BF16 MLX checkpoint convention for Qwen3-ASR.

**Verification:** `.venv/bin/python -m pytest tests/unit/test_qwen3_asr_checkpoint_unit.py tests/checkpoint/test_qwen3_asr_checkpoint.py`

### Slice 8: ASR Adapter, Registry, And CLI Language Semantics

**Objective:** Wire Qwen3-ASR into the public ASR interface and make auto language reachable from Python API and CLI.

**Acceptance criteria:**
- `_resolve_asr_family()` resolves `model_type == "qwen3_asr"` and still resolves Cohere/Granite.
- `mlx_speech.asr.load(local_qwen_path)` returns a Qwen adapter.
- Shared ASR protocol accepts `language: str | None = None`.
- Cohere and Granite map omitted language to their current English default internally.
- CLI default omits forced language; `--language auto` reaches Qwen auto mode; explicit language still forces Qwen.
- The shared `language=None` semantics change is kept isolated from Qwen model/runtime implementation so it can be reviewed and reverted independently if needed.

**Depends on:** Slices 1, 3, 6.

**Touches:** `src/mlx_speech/asr/_adapter.py`, `src/mlx_speech/asr/_registry.py`, `src/mlx_speech/asr/__init__.py`, `src/mlx_speech/asr/generate.py`, `src/mlx_speech/asr/_adapters/qwen3.py`, Cohere/Granite adapter language default handling, `tests/unit/test_asr_registry.py`, `tests/unit/test_qwen3_asr_adapter.py`, ASR CLI tests.

**Produces:** Public Qwen ASR adapter and behavior-preserving language semantics.

**Verification:** `.venv/bin/python -m pytest tests/unit/test_asr_registry.py tests/unit/test_qwen3_asr_adapter.py tests/unit/test_granite_speech_adapter.py tests/test_cohere_asr_generation.py`

### Slice 9: Dependency Guard And Packaging Surface

**Objective:** Lock the dependency boundary and exported package surface for Qwen3-ASR.

**Acceptance criteria:**
- Runtime dependency guards explicitly ban `torch`, `torchaudio`, `transformers`, `vLLM`, `librosa`, `qwen_asr`, and upstream `qwen-asr` imports in `src/`.
- Public imports expose Qwen3-ASR only through intended ASR/model modules.
- `pyproject.toml` dependencies remain unchanged unless a proven pure-runtime dependency is necessary and approved.

**Depends on:** Slices 1-8.

**Touches:** package `__init__.py` files, dependency guard tests.

**Produces:** Runtime purity guard coverage for the new family.

**Verification:** `.venv/bin/python -m pytest tests/test_runtime_purity.py tests/unit/test_granite_speech_dependency_guard.py`

### Slice 10: Documentation And Reference Notes

**Objective:** Document Qwen3-ASR v0 behavior, local model layout, BF16 checkpoint policy, language semantics, and deferred scope.

**Acceptance criteria:**
- Docs name `models/qwen3_asr_1_7b/original/` and `models/qwen3_asr_1_7b/mlx-bf16/`.
- Docs explain English/Chinese/mixed default behavior and explicit language forcing.
- Docs state v0 is single-pass offline and defers streaming, timestamps, long-audio chunking, automatic download, broader multilingual validation, and low-bit quantization.
- Reference docs keep `.references/Qwen3-ASR` as source-only evidence.

**Depends on:** Slices 7-9.

**Touches:** `docs/qwen3-asr.md`, `README.md`, `docs/references.md`.

**Produces:** User and maintainer docs for Qwen3-ASR v0.

**Verification:** `.venv/bin/python -m pytest tests/unit/`

### Slice 11: Local Runtime Smoke

**Objective:** Validate end-to-end local transcription when `mlx-bf16` checkpoint and sample audio are present.

**Acceptance criteria:**
- Runtime smoke skips when `models/qwen3_asr_1_7b/mlx-bf16/` or sample audio is absent.
- With local files, short English, Chinese, and mixed Chinese/English samples produce non-empty text.
- Default Qwen generation does not force English through Python API or CLI.

**Depends on:** Slices 1-10.

**Touches:** `tests/runtime/test_qwen3_asr_smoke.py`, optional local sample paths under ignored `models/` or `outputs/`.

**Produces:** Runtime-level proof for AC8 and AC9 when local assets exist.

**Verification:** `.venv/bin/python -m pytest tests/runtime/test_qwen3_asr_smoke.py`

## Aggregate Verification Commands
| Scope | Command |
| --- | --- |
| Unit gate after each code slice | `.venv/bin/python -m pytest tests/unit/` |
| Checkpoint gate after Slice 7 | `.venv/bin/python -m pytest tests/unit/ tests/checkpoint/` |
| Runtime smoke after Slice 11 | `.venv/bin/python -m pytest tests/runtime/test_qwen3_asr_smoke.py` |
| Final default gate | `.venv/bin/python -m pytest tests/unit/` |

## Risks Carried Into Execution
- WhisperFeatureExtractor parity is exact enough to deserve reference-vector tests before runtime work.
- Qwen3 decoder is not VibeVoice Qwen2; QK norm, GQA, RoPE, and cache behavior must be implemented from Qwen3-ASR reference evidence.
- Conv2D checkpoint layout must be proven by shape/alignment tests before loading the full BF16 model.
- Full AC8/AC9 completion depends on the local `mlx-bf16` checkpoint and suitable local English/Chinese/mixed samples.

## Handoff
Recommended next step: `auto-eng-review` before `auto-execute`, because Slices 4-7 introduce a new multimodal model runtime, checkpoint conversion path, and shared ASR language semantics.

## Review: Engineering

- Verdict: approved_with_risks
- Strength: The plan clones the already-proven Granite Speech multimodal-ASR pipeline with explicit context preflight and placeholder-mismatch guards, keeping Qwen3-ASR isolated and additive.
- Concern: Slice 9's dependency guard does not yet ban `librosa` or `qwen_asr`, and Slice 2 verifies feature shape parity but not numeric Whisper feature value parity.
- Action: In Slice 9 add `librosa` and `qwen_asr` to the banned-import set, in Slice 2 add an offline-captured reference feature-vector tolerance test, and land the Slice 8 shared `language=None` semantics change as a separable commit.
- Verified: Read SPEC/DESIGN/PLAN plus ASR registry, adapter, CLI, Cohere and Granite adapters, Granite generation runtime, VibeVoice Qwen2 decoder, Granite feature extractor, dependency-guard tests, and the Qwen3-ASR upstream inference reference.
