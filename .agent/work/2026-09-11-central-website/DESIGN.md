# Website ownership

The website source is `sites/mlx-speech/public/` in
`appautomaton/appautomaton.github.io`. Its shared build owns page, link, model
catalog, and publication checks. This repository owns runtime documentation and
README consistency checks, without a network dependency on the frontend build.
