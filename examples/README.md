# Examples

Small usage examples for `mlx-voice` will live here as the public API takes
shape.

Current v1 material:

- `clone_eval/` — fixed clone-eval metadata for the macOS built-in English
  reference set used in v1 quality work
